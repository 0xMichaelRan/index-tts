[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "indextts-worker"
version = "1.0.0"
description = "Personal TTS Worker: FastAPI + RabbitMQ integration for IndexTTS-1.5 zero-shot text-to-speech"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "0xmichaelran", email = "michaelran16@gmail.com"}
]
keywords = ["TTS", "text-to-speech", "zero-shot", "voice-cloning", "fastapi", "rabbitmq", "worker", "microservice"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Operating System :: OS Independent",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Multimedia :: Sound/Audio",
    "License :: OSI Approved :: Apache Software License",
    "Development Status :: 4 - Beta",
]

dependencies = [
    # Cross-platform common dependencies only
    # Text processing
    "cn2an==0.5.22",
    "g2p-en==2.1.0",
    "jieba==0.42.1",
    
    # Audio processing utilities
    "ffmpeg-python==0.2.0",
    "librosa",
    "sentencepiece",
    
    # Data processing
    "Cython==3.0.7",
    "numpy==1.26.2",
    "pandas==2.1.3",
    "omegaconf",
    
    # Utilities
    "tqdm",
    
    # Platform-specific text processing
    "wetext; sys_platform == 'darwin'",
    "WeTextProcessing; sys_platform != 'darwin'",
]

[project.optional-dependencies]
# Windows CUDA environment - Full GPU inference with IndexTTS models
cuda = [
    "torch>=2.1.2",
    "torchaudio",
    "transformers==4.36.2",
    "tokenizers==0.15.0",
    "accelerate==0.25.0",
    "einops==0.8.1",
    "vocos==0.1.0",
    "numba==0.58.1",
    "keras==2.9.0",
    "tensorboard==2.9.1",
    "matplotlib==3.8.2",
    "opencv-python==4.9.0.80",
]

# macOS native TTS - Lightweight testing without GPU dependencies
mac = [
    # Minimal dependencies for macOS native TTS testing
    # No PyTorch/transformers required - uses macOS system TTS
]

# REST API and RabbitMQ worker
worker = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "pika",
    "python-multipart",
]

# Gradio web UI
webui = [
    "gradio",
]

# Development tools
dev = [
    "pytest",
    "black",
    "ruff",
    "mypy",
]

# Platform-specific all installations
all-windows = [
    "indextts-worker[cuda,worker,webui,dev]",
]

all-mac = [
    "indextts-worker[mac,worker,webui,dev]",
]

# Legacy all option (defaults to CUDA for backwards compatibility)
all = [
    "indextts-worker[cuda,worker,webui,dev]",
]

[project.urls]
Homepage = "https://github.com/0xmichaelran/indexTTS-worker"
Repository = "https://github.com/0xmichaelran/indexTTS-worker"
Documentation = "https://github.com/0xmichaelran/indexTTS-worker/blob/main/README.md"
Issues = "https://github.com/0xmichaelran/indexTTS-worker/issues"
"Source (Original IndexTTS)" = "https://github.com/index-tts/index-tts"

[project.scripts]
indextts = "indextts.cli:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["indextts*"]
exclude = ["tests*"]
